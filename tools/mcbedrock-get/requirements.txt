# Pure-wheel install: no compiler needed on a stock Windows Python.
# Downloading is done by gplaydl inside WSL, so there is no Play client here —
# only Google sign-in, which is all this app needs to do itself.
gpsoauth==2.0.0
pywebview==6.2.1
pyinstaller==6.22.0
